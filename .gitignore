# --- Secrets / credentials (NEVER commit) ---
.env
.env.*
!.env.example
credentials.json
tokens/
*.token.json

# --- Local data (financial / runtime) ---
data/
ai_usage.json
app.log
*.log
temp/
temp_*.jpg
*.slip.jpg

# --- Scratch / experiments (ไม่เอาขึ้น repo) ---
scratch/
scratch*.js

# --- Deps / build / platform ---
node_modules/
.vercel
.DS_Store
